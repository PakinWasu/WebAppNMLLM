# Docker Compose Commands - Quick Reference

## ⚠️ สิ่งสำคัญ
**ต้องใช้ `-f docker-compose.prod.yml` สำหรับ Production!**

## คำสั่งพื้นฐาน

# Start services (Production)
docker compose -f docker-compose.prod.yml up -d

# Stop services
docker compose -f docker-compose.prod.yml down

# Restart services
docker compose -f docker-compose.prod.yml restart

# ดูสถานะ services
docker compose -f docker-compose.prod.yml ps

# ดู logs
docker compose -f docker-compose.prod.yml logs -f [service-name]

# Restart service เฉพาะ
docker compose -f docker-compose.prod.yml restart [service-name]

# Rebuild และ restart service
docker compose -f docker-compose.prod.yml up -d --build [service-name]

# Rebuild ทั้งหมด
docker compose -f docker-compose.prod.yml build

# Restart ทั้งหมด
docker compose -f docker-compose.prod.yml restart

## การ Update Code

# Update Backend
docker compose -f docker-compose.prod.yml build backend
docker compose -f docker-compose.prod.yml up -d backend

# Update Frontend
docker compose -f docker-compose.prod.yml build frontend
docker compose -f docker-compose.prod.yml up -d frontend

# Update ทั้งหมด (Clean)
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d

## ❌ คำสั่งที่ห้ามใช้ (ถ้าไม่ใช่ development)
# docker compose up -d          # ❌ ใช้ docker-compose.yml (development)
# docker compose down           # ❌ ใช้ docker-compose.yml (development)

## 📚 ดูเอกสารเพิ่มเติม
# DOCKER_COMMANDS.md - คู่มือคำสั่งแบบละเอียด
