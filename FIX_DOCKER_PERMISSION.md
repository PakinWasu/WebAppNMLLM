# 🔧 แก้ไขปัญหา Docker Permission

## ปัญหา: Permission denied while trying to connect to the Docker daemon socket

### วิธีแก้ไข (เลือกวิธีใดวิธีหนึ่ง):

### วิธีที่ 1: เพิ่ม User เข้า Docker Group (แนะนำ)

```bash
# เพิ่ม user ปัจจุบันเข้า docker group
sudo usermod -aG docker $USER

# ออกจาก session และ login ใหม่ หรือใช้คำสั่งนี้:
newgrp docker

# ตรวจสอบว่าเข้า group แล้ว
groups | grep docker
```

หลังจากนั้นรันสคริปต์อีกครั้ง:
```bash
./setup-and-start.sh
```

### วิธีที่ 2: ใช้ sudo (ถ้าวิธีที่ 1 ไม่ได้)

```bash
sudo ./setup-and-start.sh
```

**หมายเหตุ**: เมื่อใช้ sudo ไฟล์ที่สร้างจะเป็นของ root อาจต้องแก้ไข ownership ภายหลัง

### วิธีที่ 3: รันคำสั่ง Docker โดยตรง

```bash
# ใช้ sudo กับ docker commands
sudo docker-compose up -d --build

# หรือถ้าใช้ docker compose v2
sudo docker compose up -d --build
```

## หลังจากแก้ไข Permission แล้ว

รันสคริปต์ setup อีกครั้ง:
```bash
./setup-and-start.sh
```

## ตรวจสอบว่า Docker ทำงาน

```bash
# ตรวจสอบ Docker daemon
sudo systemctl status docker

# ตรวจสอบว่า user อยู่ใน docker group
groups | grep docker

# ทดสอบ Docker
docker ps
```
