# คู่มือการตั้งค่าและใช้งาน LLM (Ollama) บนเซิร์ฟเวอร์

## 📋 ภาพรวม

โปรเจกต์นี้ใช้ **Ollama** เป็น LLM (Large Language Model) สำหรับวิเคราะห์ network configuration

- **แบบ Remote LLM**: Backend ใน Docker ชี้ไปที่ LLM server นอกเครื่อง (เช่น `http://10.4.15.152:11434`) — ตั้งใน `backend/.env` เป็น `OLLAMA_BASE_URL=http://10.4.15.152:11434`
- **แบบ Docker**: Backend ใช้ Ollama container ใน Docker เดียวกัน — ตั้ง `OLLAMA_BASE_URL=http://ollama:11434`

## 🏗️ สถาปัตยกรรม

```
┌─────────────────────────────────────────┐
│         Docker Network                  │
│                                         │
│  ┌──────────┐      ┌──────────┐        │
│  │ Backend  │──────│  Ollama  │        │
│  │          │      │          │        │
│  └──────────┘      └──────────┘        │
│       │                  │             │
│       └──────────────────┘             │
│                                         │
└─────────────────────────────────────────┘
```

- **Backend** เชื่อมต่อกับ **Ollama** ผ่าน Docker network (`http://ollama:11434`)
- ทั้งสองรันใน Docker container เดียวกัน
- ข้อมูลโมเดลเก็บใน Docker volume (`ollama-data`)

## 🚀 การติดตั้งและตั้งค่า

### 1. ตรวจสอบ Docker Compose Configuration

โปรเจกต์มี Ollama service อยู่ใน `docker-compose.yml` และ `docker-compose.prod.yml` แล้ว:

```yaml
ollama:
  image: ollama/ollama:latest
  container_name: mnp-ollama-prod
  restart: unless-stopped
  ports:
    - "11434:11434"
  volumes:
    - ollama-data:/root/.ollama
  networks:
    - mnp-network
```

### 2. ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend/`:

```bash
cd backend
cp .env.example .env
nano .env
```

ตั้งค่าดังนี้:

```env
# AI Model Configuration (Ollama)
# สำหรับ Docker: ใช้ http://ollama:11434 (เชื่อมต่อกับ Ollama container)
# Qwen2.5-coder:32b - โมเดลเฉพาะทางสำหรับงาน technical analysis (แนะนำ)
AI_MODEL_NAME=qwen2.5-coder:32b
AI_MODEL_VERSION=v2-coder-32b
AI_MODEL_ENDPOINT=http://ollama:11434
```

**สำคัญ**: 
- สำหรับ **Production/Development ใน Docker**: ใช้ `http://ollama:11434`
- สำหรับ **Development บน Host Machine**: ใช้ `http://host.docker.internal:11434` (ถ้า Ollama รันบน host)
- สำหรับ **Remote LLM Server** (เช่น 10.4.15.152): ใช้ `OLLAMA_BASE_URL=http://10.4.15.152:11434` และให้เครื่อง backend (หรือ Docker host) ไปถึง IP นั้นได้ บน LLM server ต้องมีโมเดลที่ใช้ (เช่น `ollama pull deepseek-coder-v2:16b`) ตรงกับ `OLLAMA_MODEL` ใน `.env`

### 3. ดาวน์โหลดโมเดล LLM

หลังจาก start services แล้ว ให้ดาวน์โหลดโมเดล:

```bash
# เข้าไปใน Ollama container
docker exec -it mnp-ollama-prod bash

# หรือใช้คำสั่งนี้โดยตรง
docker exec -it mnp-ollama-prod ollama pull qwen2.5-coder:32b
```

**หมายเหตุ**: การดาวน์โหลดโมเดลครั้งแรกอาจใช้เวลานาน (ขึ้นอยู่กับขนาดโมเดลและความเร็วอินเทอร์เน็ต)

### 4. ตรวจสอบการทำงาน

```bash
# ตรวจสอบว่า Ollama container ทำงาน
docker ps | grep ollama

# ตรวจสอบ logs
docker logs mnp-ollama-prod

# ทดสอบการเชื่อมต่อจาก Backend container
docker exec -it mnp-backend-prod curl http://ollama:11434/api/tags

# ทดสอบการเรียกใช้โมเดล
docker exec -it mnp-ollama-prod ollama run qwen2.5-coder:32b "Hello, how are you?"
```

## 📝 การใช้งาน

### เริ่มต้น Services

```bash
# Development
docker-compose up -d

# Production
docker-compose -f docker-compose.prod.yml up -d
```

### ตรวจสอบสถานะ Ollama

```bash
# ดู logs แบบ real-time
docker logs -f mnp-ollama-prod

# ตรวจสอบว่าโมเดลพร้อมใช้งาน
curl http://localhost:11434/api/tags
```

### เปลี่ยนโมเดล LLM

1. แก้ไข `.env`:
   ```env
   AI_MODEL_NAME=llama3:8b  # เปลี่ยนเป็นโมเดลที่ต้องการ
   ```

2. ดาวน์โหลดโมเดลใหม่:
   ```bash
   docker exec -it mnp-ollama-prod ollama pull llama3:8b
   ```

3. Restart backend:
   ```bash
   docker-compose restart backend
   ```

## 🔧 การแก้ไขปัญหา

### ปัญหา: Backend ไม่สามารถเชื่อมต่อกับ Ollama ได้

**อาการ**: Error message `[ERROR] Ollama call failed: ...`

**วิธีแก้ไข**:

1. ตรวจสอบว่า Ollama container ทำงาน:
   ```bash
   docker ps | grep ollama
   ```

2. ตรวจสอบ network connectivity:
   ```bash
   docker exec -it mnp-backend-prod ping -c 3 ollama
   ```

3. ตรวจสอบ endpoint ใน `.env`:
   ```bash
   docker exec -it mnp-backend-prod env | grep AI_MODEL_ENDPOINT
   ```
   ควรแสดง: `AI_MODEL_ENDPOINT=http://ollama:11434`

4. ตรวจสอบ logs:
   ```bash
   docker logs mnp-backend-prod | grep -i ollama
   docker logs mnp-ollama-prod
   ```

### ปัญหา: โมเดลยังไม่ถูกดาวน์โหลด

**อาการ**: Error `model not found` หรือ `404 Not Found`

**วิธีแก้ไข**:

```bash
# ตรวจสอบโมเดลที่มีอยู่
docker exec -it mnp-ollama-prod ollama list

# ดาวน์โหลดโมเดล
docker exec -it mnp-ollama-prod ollama pull qwen2.5-coder:32b

# ตรวจสอบอีกครั้ง
docker exec -it mnp-ollama-prod ollama list
```

### ปัญหา: Ollama ใช้ RAM/CPU มากเกินไป

**วิธีแก้ไข**:

1. ใช้โมเดลขนาดเล็กกว่า:
   ```env
   AI_MODEL_NAME=qwen2.5-coder:14b  # แทน 32b (สมดุล)
   # หรือ
   AI_MODEL_NAME=qwen2.5-coder:7b   # แทน 32b (เร็วกว่า)
   ```

2. จำกัด resources ใน docker-compose:
   ```yaml
   ollama:
     deploy:
       resources:
         limits:
           memory: 4G
           cpus: '2'
   ```

### ปัญหา: Response ช้ามากหรือ Timeout

**วิธีแก้ไข**:

1. ตรวจสอบ logs เพื่อดูว่าเกิดอะไรขึ้น:
   ```bash
   docker logs mnp-ollama-prod --tail 50
   ```

2. ลดขนาด input (ในโค้ดมีการจำกัด original_content เป็น 5000 ตัวอักษรแล้ว)

3. ใช้โมเดลที่เร็วกว่า (แต่ความแม่นยำอาจลดลง)

4. เพิ่ม timeout ใน `llm_service.py` (ปัจจุบัน 600 วินาที)

## 🎯 โมเดลที่แนะนำสำหรับ Network Configuration Analysis

### Qwen2.5 Coder Models (แนะนำสำหรับงาน Technical Analysis)

**Qwen2.5 Coder** เป็นโมเดลเฉพาะทางที่ออกแบบมาสำหรับงาน technical analysis, code generation, และ code reasoning โดยเฉพาะ เหมาะกับงาน Network Configuration Analysis มากที่สุด

#### สำหรับเซิร์ฟเวอร์ที่มี RAM 32GB+

- `qwen2.5-coder:32b` ⭐ (แนะนำสูงสุด) - ความแม่นยำสูงสุด, เทียบเท่า GPT-4o ในงาน technical
  - RAM: ~16-20GB
  - Model size: ~18GB
  - เหมาะกับงาน Network Configuration Analysis ที่ต้องการความแม่นยำสูงสุด
  - รองรับ structured output (JSON) ได้ดีมาก

#### สำหรับเซิร์ฟเวอร์ที่มี RAM 16-32GB

- `qwen2.5-coder:14b` - สมดุลระหว่างความแม่นยำและความเร็ว
  - RAM: ~8-10GB
  - Model size: ~8GB
  - ความแม่นยำดีมากสำหรับงาน technical

#### สำหรับเซิร์ฟเวอร์ที่มี RAM 8-16GB

- `qwen2.5-coder:7b` - เร็วกว่าแต่ความแม่นยำต่ำกว่า
  - RAM: ~4-6GB
  - Model size: ~4GB
  - ยังคงความแม่นยำดีสำหรับงาน technical

### Qwen2.5 General Models (ทางเลือก)

#### สำหรับเซิร์ฟเวอร์ที่มี RAM 8GB+

- `qwen2.5:7b` - ความแม่นยำดี, ความเร็วปานกลาง
- `llama3:8b` - ความแม่นยำดีมาก, ใช้ RAM มาก
- `mistral:7b` - ความแม่นยำดี, ความเร็วดี

#### สำหรับเซิร์ฟเวอร์ที่มี RAM 4-8GB

- `qwen2.5:3b` - ความแม่นยำปานกลาง, ความเร็วดี
- `llama3:3b` - ความแม่นยำปานกลาง, ความเร็วดี
- `phi3:mini` - ความแม่นยำปานกลาง, ความเร็วดีมาก

#### สำหรับเซิร์ฟเวอร์ที่มี RAM น้อยกว่า 4GB

- `phi3:mini` - โมเดลเล็ก, ความเร็วดี
- `tinyllama` - โมเดลเล็กมาก, ความแม่นยำต่ำ

### ทำไมต้องใช้ Qwen2.5 Coder?

1. **Specialized Training**: ถูก train เฉพาะสำหรับงาน technical/code analysis
2. **Superior Performance**: มีประสิทธิภาพเทียบเท่า GPT-4o ในงาน technical benchmarks
3. **Network Configuration Fit**: เข้าใจ configuration syntax, command structures ได้ดีกว่า
4. **Structured Output**: สร้าง structured JSON outputs ได้ดีมาก
5. **Technical Reasoning**: มีความสามารถในการวิเคราะห์และอธิบาย technical concepts ได้ดี

## 📊 การตรวจสอบ Performance

### ดู Token Usage และ Latency

ดูได้จาก API response เมื่อสร้าง analysis:

```json
{
  "llm_metrics": {
    "inference_time_ms": 1234.5,
    "token_usage": {
      "prompt_tokens": 500,
      "completion_tokens": 300,
      "total_tokens": 800
    },
    "model_name": "qwen2.5-coder:32b"
  }
}
```

### ดู Performance Metrics จาก Database

```bash
# เข้า MongoDB
docker exec -it mnp-mongo-prod mongo

# ดู performance logs
use manage_network_projects
db.performance_logs.find().sort({timestamp: -1}).limit(10).pretty()
```

## 🔒 Security Considerations

1. **ไม่เปิดเผย Ollama port ไปยัง public** (ถ้าไม่จำเป็น):
   - Port 11434 ไม่ควร expose ไปยัง public network
   - ใช้ผ่าน Docker network เท่านั้น

2. **Backup โมเดล** (ถ้าต้องการ):
   ```bash
   # Backup volume
   docker run --rm -v ollama-data:/data -v $(pwd):/backup alpine tar czf /backup/ollama-backup.tar.gz /data
   ```

3. **Monitor Resource Usage**:
   ```bash
   docker stats mnp-ollama-prod
   ```

## 📚 เอกสารเพิ่มเติม

- [Ollama Official Documentation](https://ollama.ai/docs)
- [ANALYSIS_SYSTEM.md](backend/ANALYSIS_SYSTEM.md) - รายละเอียดระบบ Analysis
- [README.md](README.md) - คู่มือหลักของโปรเจกต์

## 🆘 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ logs: `docker logs mnp-ollama-prod`
2. ตรวจสอบ network: `docker network inspect mnp-network`
3. ตรวจสอบ environment variables: `docker exec mnp-backend-prod env | grep AI`
4. ดูเอกสาร Ollama: https://ollama.ai/docs
