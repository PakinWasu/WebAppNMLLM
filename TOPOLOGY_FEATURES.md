# 🎯 ฟีเจอร์ Topology ตามขอบเขตที่ระบุ

## สรุปฟีเจอร์ที่ Implement แล้ว

### 2.3.5.6 ระบบสามารถสร้าง topology อัตโนมัติจากข้อมูล CDP/LLDP/Interfaces และ Routing

✅ **ทำเสร็จแล้ว:**
- สร้าง topology อัตโนมัติจากข้อมูล CDP/LLDP/Interfaces และ Routing
- ใช้ LLM (Qwen2.5-coder:14b) วิเคราะห์และสร้าง topology
- แสดงผล topology graph แบบ interactive

### 2.3.5.6.1 ลากตำแหน่งอุปกรณ์ได้อย่างอิสระ

✅ **ทำเสร็จแล้ว:**
- ผู้ใช้สามารถลากโหนด (อุปกรณ์) เพื่อย้ายตำแหน่งได้
- รองรับการลากในโหมดแก้ไข (Edit Mode)
- ตำแหน่งถูกบันทึกเมื่อกด "บันทึกตำแหน่งใหม่"

### 2.3.5.6.2 ลากเส้นเชื่อมโยงใหม่ (Re-route link connector)

✅ **ทำเสร็จแล้ว:**
- ผู้ใช้สามารถเปลี่ยนเส้นเชื่อมโยงได้โดย:
  1. คลิกที่ลิงก์ที่ต้องการเปลี่ยน
  2. กดปุ่ม "เปลี่ยนเส้นเชื่อมโยง"
  3. คลิกที่โหนดปลายทางใหม่
- ลิงก์จะถูกอัปเดตให้เชื่อมต่อกับโหนดใหม่

### 2.3.5.6.3 ปรับความใกล้–ไกลเพื่อจัดรูปแบบผัง

✅ **ทำเสร็จแล้ว:**
- **Zoom In/Out**: ปุ่ม 🔍+ และ 🔍- สำหรับขยาย/ย่อ
- **Mouse Wheel**: ใช้ล้อเมาส์เพื่อซูม (ในโหมดแก้ไข)
- **Pan**: กด Ctrl+ลากเพื่อเลื่อนผัง
- **Reset**: ปุ่ม 🔄 รีเซ็ตมุมมองกลับเป็นค่าเริ่มต้น
- แสดงระดับ zoom เป็นเปอร์เซ็นต์

### 2.3.5.6.4 บันทึกตำแหน่งใหม่ (Save Layout)

✅ **ทำเสร็จแล้ว:**
- ปุ่ม "บันทึกตำแหน่งใหม่" บันทึก:
  - ตำแหน่งโหนดทั้งหมด (topoPositions)
  - ลิงก์ทั้งหมด (topoLinks)
  - Labels และ Roles ของโหนด (topoNodeLabels, topoNodeRoles)
- บันทึกไปยัง Backend ผ่าน API `/projects/{project_id}/topology/layout`
- แสดงข้อความยืนยันเมื่อบันทึกสำเร็จ

### 2.3.5.6.5 ยกเลิก (Cancel) เพื่อไม่เก็บค่าการแก้ไข

✅ **ทำเสร็จแล้ว:**
- ปุ่ม "ยกเลิก" จะ:
  - รีเซ็ตตำแหน่งกลับเป็นค่าที่บันทึกไว้
  - รีเซ็ตลิงก์กลับเป็นค่าที่บันทึกไว้
  - รีเซ็ต labels และ roles กลับเป็นค่าที่บันทึกไว้
  - ปิดโหมดแก้ไข
  - รีเซ็ต zoom และ pan

## ฟีเจอร์เพิ่มเติมที่ Implement

### การจัดการลิงก์
- **เพิ่มลิงก์**: คลิก 2 โหนดเพื่อสร้างลิงก์ใหม่
- **แก้ไขลิงก์**: คลิกลิงก์เพื่อแก้ไขประเภทและป้ายกำกับ
- **ลบลิงก์**: คลิกขวาที่ลิงก์เพื่อลบ

### การจัดการโหนด
- **แก้ไขโหนด**: คลิก 2 ครั้งที่โหนดเพื่อแก้ไข
- **ดูข้อมูล**: ชี้เมาส์เพื่อดูข้อมูลโหนด (Role, Model, IP, etc.)

### การแสดงผล
- **Device Icons**: แสดงไอคอนตามประเภทอุปกรณ์ (Core, Distribution, Access, Router, Switch)
- **Link Colors**: สีลิงก์เปลี่ยนตามสถานะ (ปกติ, เลือก, กำลังเปลี่ยนเส้น)
- **Visual Feedback**: แสดงสถานะการทำงานชัดเจน

## การใช้งาน

### 1. สร้าง Topology อัตโนมัติ

1. คลิกปุ่ม "🤖 สร้าง Topology อัตโนมัติ"
2. รอให้ระบบวิเคราะห์ข้อมูล (ใช้เวลา 1-3 นาที)
3. Topology จะถูกสร้างและแสดงผล

### 2. แก้ไข Topology

1. คลิกปุ่ม "แก้ไขกราฟ"
2. **ลากโหนด**: คลิกและลากโหนดเพื่อย้ายตำแหน่ง
3. **เพิ่มลิงก์**: คลิกปุ่ม "เพิ่มลิงก์" แล้วคลิก 2 โหนด
4. **เปลี่ยนเส้นเชื่อมโยง**: 
   - คลิกลิงก์ที่ต้องการเปลี่ยน
   - คลิกปุ่ม "เปลี่ยนเส้นเชื่อมโยง"
   - คลิกที่โหนดปลายทางใหม่
5. **ปรับมุมมอง**: 
   - ใช้ปุ่ม 🔍+ / 🔍- เพื่อขยาย/ย่อ
   - ใช้ล้อเมาส์เพื่อซูม
   - กด Ctrl+ลากเพื่อเลื่อนผัง

### 3. บันทึกการแก้ไข

1. คลิกปุ่ม "บันทึกตำแหน่งใหม่"
2. รอให้ระบบบันทึก (จะแสดงข้อความยืนยัน)
3. การแก้ไขจะถูกบันทึกและแสดงผลในครั้งถัดไป

### 4. ยกเลิกการแก้ไข

1. คลิกปุ่ม "ยกเลิก"
2. ทุกอย่างจะกลับเป็นค่าที่บันทึกไว้ล่าสุด

## API Endpoints

### Generate Topology
```http
POST /projects/{project_id}/topology/generate
```

### Get Topology Layout
```http
GET /projects/{project_id}/topology
```

### Save Topology Layout
```http
PUT /projects/{project_id}/topology/layout
Content-Type: application/json

{
  "positions": {
    "device1": {"x": 30, "y": 50},
    "device2": {"x": 70, "y": 50}
  },
  "links": [
    {"a": "device1", "b": "device2", "type": "trunk", "label": ""}
  ],
  "node_labels": {"device1": "CORE1"},
  "node_roles": {"device1": "core"}
}
```

## Keyboard Shortcuts

- **Ctrl/Cmd + ลาก**: เลื่อนผัง (Pan)
- **ล้อเมาส์**: ซูม (Zoom)
- **คลิกขวาที่ลิงก์**: ลบลิงก์

## Troubleshooting

### Topology ไม่แสดงผล

1. ตรวจสอบว่าได้สร้าง topology แล้ว (คลิก "สร้าง Topology อัตโนมัติ")
2. ตรวจสอบว่า Ollama ทำงานอยู่
3. ตรวจสอบ logs: `docker logs mnp-backend-prod`

### ไม่สามารถบันทึกได้

1. ตรวจสอบว่าเป็น Manager หรือ Admin
2. ตรวจสอบ network connectivity
3. ตรวจสอบ console สำหรับ error messages

### ลิงก์ไม่แสดงผล

1. ตรวจสอบว่าได้สร้าง topology แล้ว
2. ตรวจสอบว่า devices มีข้อมูล LLDP/CDP neighbors
3. ลองสร้าง topology ใหม่

## Performance Optimizations

- ใช้โมเดล 14b แทน 32b เพื่อความเร็ว
- ลดข้อมูลที่ส่งไปให้ LLM (filtering)
- Optimize prompt structure
- Timeout เพิ่มขึ้นเป็น 450s สำหรับ topology generation
